/*Reverse string */
#include<iostream>
#include<string>

using namespace std;


string ReverseString(string str){
	int j=str.length();
	for (int i=0;i<=j/2;i++)
	   swap(str[i],str[j-i-1]);
}

int main(){
	string str;
	cin>>str;
	cout<<ReverseString(str);
	return 0;
}
